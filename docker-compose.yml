version: '3'

services:

  db:
    container_name: auth_db
    image: postgres:13-alpine
    ports:
      - 5432:5432
    restart: always
    environment:
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=temp_pwd
    - POSTGRES_DB=auth